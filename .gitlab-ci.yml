build-job:
  stage: build
  script:
    - apk add build-base && apk add make && apk add meson && apk add ninja
    - make build

test-parser:
  stage: test
  script:
    - ./tests/parser.sh

cache:
  paths:
    - 42sh
    - builddir/